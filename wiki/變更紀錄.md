# 2026-04-02 (v2.9) 更新內容

### 🔒 核心資安升級 (OWASP Security Fixes)
- **JWT 數位簽章驗證**：修補重大身分驗證漏洞 (OWASP A07)，`verifyGoogleIdToken_` 改採呼叫 Google 官方 `tokeninfo` 端點驗證密碼學簽章，防堵權限偽造。
- **防止試算表注入 (Formula Injection)**：修補資料隱碼漏洞 (OWASP A03)，實作 `sanitizeForSheet` 攔截所有 `=, +, -, @` 開頭的危險輸入，轉為安全純文字寫入 Google Sheets。

### ⚡ 後端效能與體驗優化 (Performance & UX)
- **廢套效能巨幅提升**：優化 `apiDeletePrizeLibrary` 邏輯，縮減高達 80% 的不必要資料讀取量。改進原先耗時的逐筆 `deleteRow()` 操作，重構成合併連續行號後的單次 `deleteRows(start, count)` 批次刪除，使整體執行時間從原本 10~15 秒顯著縮短至 3~5 秒。
- **JWT 快取機制**：為減輕 `tokeninfo` 端點驗證對每次 API 呼叫的額外 HTTP 開銷，導入 `CacheService` 對已驗證的 Token 提供 45 分鐘有效性快取，兼顧安全性與系統反應速度。
- **ScrollToTop 懸浮元件**：於前端全局加入右下角「回到頂部」的導覽輔助功能，便於瀏覽長篇幅商品與銷售清單。
- **值班人員整合通知**：強化 LINE 的溝通上下文，於「每日關帳通知」及「遲到打卡通知」的訊息中自動帶入並標示當日「值班人員」。

---

# 2026-04-02 (v2.8) 更新內容

### 💬 LINE Messaging API 整合
- **Sheet-based Channel 制**：通知目標改為讀取 Google Sheet「LINE通知設定」分頁，支援多 channel（`all`、`admin`、`竹北`、`金山`）與多目標推送。
- **LINE Push / Reply API**：新增 `_linePush()` 推送函式和 `_lineReply()` 回覆函式。Reply 免費不佔 Push 額度。
- **ID 指令**：在 LINE 群組輸入「ID」，bot 自動回覆 `groupId` 或 `userId`，方便設定 Sheet。
- **Webhook 整合**：`doPost()` 新增 LINE Webhook 事件偵測。
- **Channel 快取**：`CacheService` 快取 300 秒，減少 Sheet 讀取。
- **向下相容**：保留 `sendNotification()` 和 `sendAdminNotification()` 舊函式。

### 📊 關帳通知加強
- **GK 確認清單**：關帳通知新增三類 GK 項目（帶走/換點數/點數直購），含客戶姓名（從會員資料庫查詢）。
- **備註顯示**：通知含店員關帳備註。

### 📈 GAS / LINE 用量追蹤
- **計數器**：`_countUrlFetch(type)` 追蹤 Push/Reply 每日和每月用量，`_countGasCall()` 追蹤 `doPost` 呼叫次數。
- **checkQuotaUsage()**：GAS 編輯器跑此函式可查看完整用量報告。
- **前端儀表板**：大師 tab 新增 4 格用量資訊卡 + LINE Push 進度條（超過 80% 變紅色）。

### 🖥️ 大師 Tab LINE 發送
- **Channel 動態載入**：從 Sheet 載入 channel 清單。
- **訊息發送**：管理員可選擇 channel 一鍵推送 LINE 訊息。

### 🧪 測試
- 新增 14 個 LINE 相關 UT（`line-api.test.ts`），全部 229 個測試 PASS，覆蓋率 93.46%。

### 📖 文件
- **DEPLOY.md** 新增第 14 節 LINE 通知系統設定指南。

---

# 2026-04-01 (v2.7) 更新內容

### 📡 即時結帳監控系統
- **MonitorView**：新增「監控」分頁，即時顯示各門市結帳活動。
- **ScriptProperties 草稿**：結帳時自動寫入草稿，關帳時清除。
- **手動重新整理**：取代自動 polling，減少 API 呼叫。
- **監控開關記憶**：`useStickyState` 持久化 localStorage。

### 🧪 測試
- 新增 21 個監控功能 UT + Playwright e2e 測試。

---



### 🌍 多環境部署架構 (Dev / Prod Separation)
- **後端資料庫分離**：GAS 後端 (`config.js`) 全面改用 `ScriptProperties` 動態讀取 `APP_BACKGROUND_ID`。開發環境將自動 fallback 至測試用 Google Sheet，正式環境只需設定屬性即可，程式碼雙邊 100% 一致無須手動改 ID。
- **前端 CI/CD 流程升級**：修改 GitHub Actions (`.github/workflows/deploy.yml`) 規則，只有合併至 `main` 時才會觸發 GitHub Pages 正式版部署，並取用 Secrets 中的正式版 `VITE_GAS_URL`。
- **Clasp 環境設定檔拆分**：引入 `.clasp-dev.json` 與 `.clasp-prod.json`，在 `package.json` 封裝 `npm run push:dev` 與 `npm run push:prod` 腳本，一鍵切換環境並發佈新版。

---

# 2026-03-28 (v95) 更新內容

### 🔔 緊急通知系統 (Emergency Notification)
- **後端 API：** 新增 `setEmergencyNotice`、`getEmergencyNotice`、`clearEmergencyNotice` 三個 API，使用 GAS `ScriptProperties` 儲存當前通知（無需額外 Sheet）。只有管理員帳號（`ADMIN_EMAILS`）可發送/撤回通知。
- **前端輪詢機制：** 透過 `useEmergencyNotice` hook 以可設定間隔（預設 2 分鐘，`config.ts` 的 `NOTICE_POLL_MS`）輪詢通知狀態。僅在瀏覽器 tab 可見時才發請求（`document.visibilityState`），從背景回到前景時自動補查。
- **全螢幕緊急彈窗：** `EmergencyNoticeModal` — 黑色遮罩 + 紅色漸層卡片 + 警告圖示彈跳動畫。顯示通知內容、發送者、時間。店員可點「我知道了」關閉本地彈窗（不影響伺服器通知），管理員可「撤回通知」全面清除。

### 👑 大師專用分頁 (Master Tab)
- **權限控制：** 新增 `ADMIN_EMAILS` 設定（`config.ts`），僅管理員帳號可見「大師」分頁。`Tab` type 新增 `'master'`，TABS 陣列新增 Crown 圖示。
- **MasterView 元件：** 含通知狀態面板（紅色/綠色）、textarea 輸入區（限 500 字）、發送/撤回按鈕、操作回饋。發送前有確認 dialog 防誤觸。

### 📊 獎項庫抽選狀況 (Draw Status)
- **後端 API `getDrawCounts`（大師專用）：** 交叉比對「銷售紀錄」+ 兩店「當日銷售」，按 `{setId}_{prize}` 累加 `draws` 欄位（支援新舊結帳格式偵測）。計算結果同時寫回獎項庫 Sheet K 欄。
- **前端獎項庫分頁：** 永遠顯示「已抽」欄位（從 Sheet K 欄的 `drawnCount` 讀取），已抽 ≥ 總抽顯示綠色、未滿顯示紅色。管理員額外看到「查詢抽選狀況」按鈕可即時重新計算。
- **大小寫統一：** 所有 prize code 比對統一 `.toLowerCase()` + `String()` 處理，相容數字型別（如 `1`、`20`）與字母型別（如 `Z`、`x`）。

### 📋 廢套紀錄增強
- **已抽數記入廢套紀錄：** 作廢福袋時先掃描銷售紀錄計算已抽數量，日誌新增「已抽總數」欄位（I 欄），獎項明細格式升級為 `prize:name:points:已抽N`。
- **標題列更新：** 廢套紀錄 Sheet 標題列新增為 12 欄：`作廢時間 | 門市 | 操作人員 | 套號 | 套名 | 單抽價 | 獎項數 | 總抽數 | 已抽總數 | 獎項明細 | 是否點數套 | 原始建套日期`。

---

# 2026-03-28 更新內容

### 🔒 Race Condition 修復 (Concurrency Safety)
- **`todaySaleRecord.js` `deleteTodaySaleRecord()`：** 原本完全沒有 LockService 保護，刪除標記與退點操作非原子性。現已包覆 `ScriptLock`，並改用 `_addPointsUnsafe` 避免雙重拿鎖。
- **`todaySaleRecord.js` `closeAccountBox()`：** 原本使用 `DocumentLock`，與 `apiCheckout` 等 API 使用的 `ScriptLock` 不互斥，關帳搬移資料時結帳可同時寫入導致資料遺漏或被清除。已改為 `ScriptLock`，並將逐行 `appendRow` 改為批次 `setValues` 提升效能與安全性。
- **`services.js` `結帳()`：** 原本點數更新與銷售紀錄寫入使用兩把不同的鎖，若點數扣除成功但寫入銷售紀錄失敗會造成資料不一致。已合併為單一 `ScriptLock` 包覆，並改用 `_addPointsUnsafe`。
- **`addLottery.js` `福袋入檔()`：** 移除鎖外的死代碼 `finalId = getMaxValueOfColumnA() + 1`（該值已在鎖內被 `actualFinalId` 覆寫）。

### 🎨 UI/UX 統一刷新按鈕 & 分頁持久化
- **統一 `RefreshButton` 元件：** 新增 `RefreshButton.tsx`，全分頁共用統一的刷新資料按鈕（indigo→violet 漸層 + loading 脈衝動畫 + 旋轉圖示 + 文字切換「讀取中…」）。已套用至：當日銷售、會員資料、歷史銷售紀錄、福袋獎項庫、貨品資料庫、盲盒資料庫。
- **瀏覽器重整保持分頁：** `activeTab` 與 `branch` 改用 `useStickyState` 持久化至 localStorage，F5 重整後自動留在當前分頁與門市並重新載入資料。
- **銷售紀錄 F5 強制重抓：** 初次載入時跳過 localStorage cache 直接重抓最新 1000 筆；切換 tab 再回來則使用 cache。
- **移除關帳 summary row：** `apiCloseDay` 不再產生 `【系統結帳紀錄】` 的 summary 行。
- **修復浮點精度顯示：** 在 `gasPost()` 加入 `roundFields()` 遞迴處理，對 `points`、`pointDelta`、`amount`、`cash` 等數值欄位統一 `Math.round()`，消除 `110.00000000000001` 這類 IEEE 754 誤差。
- **銷售紀錄顯示會員名字：** 歷史銷售紀錄頁面的交易 header 現在會根據電話號碼查找並顯示會員姓名。

### 📋 後端操作紀錄 (Audit Logging)
- **開關帳紀錄 sheet（12 欄）：** 關帳成功後自動寫入一列紀錄，包含：關帳時間、門市、操作人員 email、開櫃準備金、應收/實收現金、現金差異、信用卡總額、匯款總額、本日營業額、交易筆數、備註。首次寫入時自動補標題列。
- **廢套紀錄 sheet（11 欄）：** 作廢獎項庫後自動寫入一列紀錄，包含：作廢時間、門市、操作人員 email、套號、套名、單抽價、獎項數、總抽數、獎項明細（分號分隔）、是否點數套、原始建套日期。
- **後端浮點修復：** GAS 後端在產生廢套明細時，對 `points` 欄位加入 `Math.round()` 避免浮點誤差寫入紀錄。

---

# 2026-03-19 更新內容

### 🎨 介面與體驗優化 (UI/UX Improvements)
- **結帳頁面版面微調：** 大幅壓縮「福袋清單」與「直購商品清單」的次要資訊（如單號、數量、單價）欄位寬度，並將資源留給「大套名稱」和「商品名稱」，優先突顯最重要的資訊。
- **編輯狀態視覺化：** 針對所有輸入框進行重構，清楚區分「可編輯」與「唯讀」欄位。唯讀欄位以褪色且無外框的方式呈現，降低視覺干擾。
- **貨品資料頁面大翻新：** 捨棄過時的灰色表頭與死板的警告色塊。導入現代化圓角搜尋卡片、整合式搜尋圖示，並使用柔和的徽章取代突兀的警告標語，優化資訊層次。
- **響應式佈局 (RWD)：** 解除結帳頁面的最大寬度限制，讓大螢幕能完整展開。同時為表格加入防擠壓設計 (`min-w-[800px]`)，確保手機版滑動體驗順暢。

### ⚡ 結帳操作與防呆 (Checkout & Safety)
- **結帳資料防丟失機制：** 結帳畫面所有輸入狀態（顧客資料、福袋明細、商品清單、收款金額）皆透過 `useStickyState` 寫入本機快取。即使不小心按到 F5 重新整理，資料也原封不動保留。
- **一鍵重置結帳：** 於結帳按鈕旁新增「全新結帳」功能，防呆確認後可瞬間淨空畫面上所有快取資料，方便迎接下一位顧客。
- **會員輸入輔助：** 會員電話輸入支援自動完成，點選快取清單後會順帶在欄位下方顯示該會員「姓名」以供雙重核對。
- **特定獎項 (Z) 邏輯處理：** 當福袋獎項輸入大寫或小寫 `Z` 時，名稱自動帶入「非GK」，點數預設為 `1`。客戶選「帶走」時無點數，選「點數」則依抽數計算總點數。
- **GK 直購鎖定防呆：** 掃入貨品資料庫的 ID 時，系統會自動判斷該商品是否屬於 GK 類別。 若為 GK，結帳付款清單將**強制隱藏「現金」選項**（包含防呆自動切換為點數），避免誤用現金結帳。盲盒則正常開放手動售價與現金付款。

### 📜 歷史紀錄與相容性強化 (History & Compatibility)
- **消費紀錄資訊全透明：** 針對福袋明細，於單號下方明確標示顧客選擇「帶走」或「點數」；若是點數兌換亦會顯示增加/扣除之額外點數，清楚無比。
- **直購扣點標籤：** 當直購商品（含隨便扣 88888、99999）付款方式為點數兌換時，現在明細會追加紫色的 `點數兌換` 與結算 `扣 XX 點` 的醒目標籤，一眼看穿金流與點流。
- **舊版資料相容重生 (`web_api.js`)：** 為了找回過去舊版結帳流程中消失的點數扣抵數據，後端全面重寫了解析核心，讓成千上萬筆歷史紀錄「自動轉譯」成新版框架能讀懂的乾淨格式。
- **福袋獎項點數顯示優化：** 解決 Google 試算表帶來的浮點數小數點過長問題（例如 `110.00000001`），自動進位並隱藏多餘尾數。
- **資料檢索防呆：** 修復在搜尋列輸入特定會員姓名或字元時導致網頁崩潰 (Crash) 的型別錯誤 Bug。
